Currently, we manage events via the CMS.

Ideally, we want ALL content on site to be managed via CMS. 
E.g nav titles and links
Section text content, images, links etc. 
Form field labels
CTA text, links 
All copy

This should all be implemented in a clean componentised way, we don't need to be able to reorder sections at this point, just control their content. 
Use best practices for Astro components, TailwindCSS and Keychain CMS for implementing this. 
Ensure the defaults are the same as the current hard coded build. 